version: "3"
services:
  nginx:
    volumes:
      - ./docker/nginx/ssl:/etc/nginx/ssl

  php-fpm:
    volumes:
      - ./docker/php-fpm/etc/docker-php-ext-xdebug.ini:/usr/local/etc/php/conf.d/docker-php-ext-xdebug.ini # additional php configuration file

  mariadb:
    ports:
      - "33060:3306"
